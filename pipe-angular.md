# Pipe
**I Angular templates bruges pipes til at transformere data til visning. Det er en slags “formatering” i HTML.**

```html
{{ value | pipeName:arg1:arg2 }}
```
- value → det data, du vil transformere
- pipeName → navnet på pipen (fx date, uppercase, currency)
- arg1, arg2… → valgfrie argumenter
